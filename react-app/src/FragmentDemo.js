import React from 'react'

function FragmentDemo() {
    return (
        <React.Fragment>
            <h1>Fragment Demo</h1>
            <p>This descibes the FragmentDemo component</p>
        </React.Fragment> // it removes the div tag in the DOM tree
    )
}

export default FragmentDemo
